class Devise::UnlocksController < DeviseController
  # GET /resource/unlock/new
  def new: () -> untyped

  # POST /resource/unlock
  def create: () { (untyped) -> untyped } -> untyped

  # GET /resource/unlock?unlock_token=abcdef
  def show: () { (untyped) -> untyped } -> untyped

  # The path used after sending unlock password instructions
  def after_sending_unlock_instructions_path_for: (untyped resource) -> (untyped | nil)

  # The path used after unlocking the resource
  def after_unlock_path_for: (untyped resource) -> (untyped | nil)

  def translation_scope: () -> "devise.unlocks"
end
